name: CI Orchestrator

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution Mode'
        required: true
        default: 'Economy'
        type: choice
        options:
        - Economy
        - Full

jobs:
  # Label PRs based on changed files
  label-pr:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@v5
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          sync-labels: true

  # Determine if we should run expensive tests
  decision:
    runs-on: ubuntu-latest
    outputs:
      run_e2e: ${{ steps.calc.outputs.run_e2e }}
    steps:
      - id: calc
        run: |
          if [[ "${{ github.event_name }}" == "release" || "${{ inputs.mode }}" == "Full" || "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "run_e2e=true" >> $GITHUB_OUTPUT
          else
            echo "run_e2e=false" >> $GITHUB_OUTPUT
          fi

  lint:
    uses: ./.github/workflows/lint.yml

  test-backend:
    needs: lint
    uses: ./.github/workflows/test.yml
    with:
      component: backend

  test-frontend:
    needs: lint
    uses: ./.github/workflows/test.yml
    with:
      component: frontend

  build:
    needs: [test-backend, test-frontend]
    uses: ./.github/workflows/build.yml

  e2e:
    needs: build
    if: needs.decision.outputs.run_e2e == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Run Playwright
        run: echo "Running Playwright E2E tests..."
        # In scaffolding, we just echo. Real impl would start containers and run pytest/playwright
