name: Maintenance Cleanup

on:
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old artifacts
        uses: actions/github-script@v8
        with:
          script: |
            // Logic to clean up old artifacts using github rest api
            console.log("Cleaning up old artifacts...");
